--Problem 2 - Product Price Listing by Category

USE StoreDB;

--Create Categories Table
CREATE TABLE categories (
    category_id INT PRIMARY KEY,
    category_name VARCHAR(100)
);

--Create Brands Table
CREATE TABLE brands (
    brand_id INT PRIMARY KEY,
    brand_name VARCHAR(100)
);

--Create Products Table
CREATE TABLE products (
    product_id INT PRIMARY KEY,
    product_name VARCHAR(100),
    brand_id INT,
    category_id INT,
    model_year INT,
    list_price DECIMAL(10,2),

    FOREIGN KEY (brand_id) REFERENCES brands(brand_id),
    FOREIGN KEY (category_id) REFERENCES categories(category_id)
);

--Insert Sample Data
--1.Categories
INSERT INTO categories VALUES
(1, 'Electronics'),
(2, 'Clothing'),
(3, 'Sports');

--2.Brands
INSERT INTO brands VALUES
(1, 'Nike'),
(2, 'Samsung'),
(3, 'Adidas');

--Products
INSERT INTO products VALUES
(101, 'Running Shoes', 1, 3, 2024, 750),
(102, 'Smart TV', 2, 1, 2025, 45000),
(103, 'T-Shirt', 3, 2, 2024, 400),
(104, 'Headphones', 2, 1, 2023, 1200);

--Display:product_name,brand_name,category_name,model_year,list_price

SELECT p.product_name,b.brand_name,c.category_name,p.model_year,p.list_price
FROM products p

--Use:INNER JOIN
INNER JOIN brands b ON p.brand_id = b.brand_id
INNER JOIN categories c ON p.category_id = c.category_id

--Filter:list_price > 500
WHERE p.list_price > 500

--Sort:list_price ASC
ORDER BY p.list_price ASC;
